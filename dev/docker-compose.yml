# Development Environment for TopClaw Agentic Testing
#
# Use this for:
# - Running the agent in a sandboxed environment
# - Testing dangerous commands safely
# - Developing new skills/integrations
#
# Usage:
#   cd dev && ./cli.sh up
#   or from root: ./dev/cli.sh up
name: topclaw-dev
services:
  # ── The Agent (Development Image) ──
  # Builds from source using the 'dev' stage of the root Dockerfile
  topclaw-dev:
    build:
      context: ..
      dockerfile: Dockerfile
      target: dev
    container_name: topclaw-dev
    restart: unless-stopped
    environment:
      - TOPCLAW_GATEWAY_PORT=42617
      - SANDBOX_HOST=topclaw-sandbox
    secrets:
      - source: topclaw_env
        target: topclaw_env
    entrypoint: ["/bin/bash", "-lc"]
    command:
      - |
        if [ -f /run/secrets/topclaw_env ]; then
          set -a
          . /run/secrets/topclaw_env
          set +a
        fi
        exec topclaw gateway --port "${TOPCLAW_GATEWAY_PORT:-42617}" --host "[::]"
    volumes:
      # Mount single config file (avoids shadowing other files in .topclaw)
      - ../target/.topclaw/config.toml:/topclaw-data/.topclaw/config.toml
      # Mount shared workspace
      - ../playground:/topclaw-data/workspace
    ports:
      - "127.0.0.1:42617:42617"
    networks:
      - dev-net

  # ── The Sandbox (Ubuntu Environment) ──
  # A fully loaded Ubuntu environment for the agent to play in.
  sandbox:
    build:
      context: sandbox # Context relative to dev/
      dockerfile: Dockerfile
    container_name: topclaw-sandbox
    hostname: dev-box
    command: ["tail", "-f", "/dev/null"]
    working_dir: /home/developer/workspace
    user: developer
    environment:
      - TERM=xterm-256color
      - SHELL=/bin/bash
    volumes:
      - ../playground:/home/developer/workspace # Mount local playground
    networks:
      - dev-net

networks:
  dev-net:
    driver: bridge

secrets:
  topclaw_env:
    file: ../.env
