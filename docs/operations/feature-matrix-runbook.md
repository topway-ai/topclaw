# Feature Matrix Runbook

> **Removed in v2026.3.22.** The `feature-matrix.yml` workflow and its `whatsapp-web`, `browser-native`, and `nightly-all-features` lanes were removed during the simplification sprint. See `docs/audit-2026-03-22-simplification.md` for history.

Current CI validation is handled by `.github/workflows/ci-run.yml`.
