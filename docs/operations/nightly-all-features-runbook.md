# Nightly All-Features Runbook

> **Removed in v2026.3.22.** The `feature-matrix.yml` nightly profile and `nightly-all-features.yml` workflow were removed during the simplification sprint. See `docs/audit-2026-03-22-simplification.md` for history.

Current CI validation is handled by `.github/workflows/ci-run.yml`.
