# Release Reset v2026.3.20 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reset TopClaw version metadata on `main` back to `2026.3.20`, correct documentation that no longer matches the current channel build defaults, and re-establish `v2026.3.20` as the release tag that can validly publish from the current codebase.

**Architecture:** Keep the change set narrow. Update only the version metadata and stale documentation that conflicts with the current codebase, then verify the release/tag guard assumptions before integrating and recreating the release tag from the corrected `main`.

**Tech Stack:** Rust package metadata, Markdown docs, Git/GitHub release workflow, Python-based CI guard scripts

---

### Task 1: Reset Version Metadata

**Files:**
- Modify: `Cargo.toml`
- Modify: `Cargo.lock`

- [ ] **Step 1: Update package version metadata**

Set the package version back from `2026.3.22` to `2026.3.20` in the Cargo metadata files that ship the release identity.

- [ ] **Step 2: Verify version references**

Run: `rg -n "2026\\.3\\.22|2026\\.3\\.21|2026\\.3\\.20" Cargo.toml Cargo.lock`
Expected: only `2026.3.20` remains in release metadata files.

- [ ] **Step 3: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "chore: reset version metadata to 2026.3.20"
```

### Task 2: Correct Stale Documentation

**Files:**
- Modify: `docs/channels-reference.md`
- Review: `docs/**`

- [ ] **Step 1: Audit stale docs references**

Use repository search to confirm which docs still conflict with the current codebase, especially around channel build defaults and release-version wording.

- [ ] **Step 2: Update only inconsistent docs**

Correct the stale statement that still claims default builds include `channel-lark`, and leave unchanged docs untouched.

- [ ] **Step 3: Verify docs search results**

Run: `rg -n "Default builds include|default = \\[\\\"channel-|2026\\.3\\.22|2026\\.3\\.21" docs README.md Cargo.toml`
Expected: no stale default-build or release-version mismatches remain.

- [ ] **Step 4: Commit**

```bash
git add docs/channels-reference.md
git commit -m "docs: align channel build defaults with code"
```

### Task 3: Verify Release Guard Compatibility

**Files:**
- Review: `scripts/ci/release_trigger_guard.py`
- Review: `.github/workflows/pub-release.yml`

- [ ] **Step 1: Run release compatibility checks**

Run targeted verification that the current metadata and tag plan are compatible with the repository’s release guard assumptions.

- [ ] **Step 2: Run project verification**

Run:
- `cargo fmt --all -- --check`
- `cargo test --no-default-features --features channel-telegram,channel-discord,channel-lark`

Expected: all checks pass on the rollback metadata state.

- [ ] **Step 3: Commit if verification required follow-up edits**

```bash
git add -A
git commit -m "test: verify release reset compatibility"
```

### Task 4: Integrate and Recreate Release Tag

**Files:**
- Modify remote Git refs / release state after merge

- [ ] **Step 1: Merge the branch back to main**

Use the repository’s PR flow, wait for required checks, merge, and clean up the temporary branch/worktree.

- [ ] **Step 2: Recreate the release tag**

Delete and recreate `v2026.3.20` on the corrected `main` only after the merge is complete and the release metadata matches the tag version.

- [ ] **Step 3: Trigger release publish**

Use the repository’s existing publish path for the existing tag and verify the release assets are attached to `v2026.3.20`.
